from Graph import Graph
import matplotlib.pyplot as plt
from GraphAlgo import GraphAlgo
from RangeClasses import Range
from RangeClasses import Range2D
from RangeClasses import Range2Range

# if __name__ == '__main__':
    # graph = Graph()
    # graph.add_node(1,(1,1,2))
    # graph.add_node(2,(2,2,2))
    # graph.add_node(3, (3, 1, 2))
    # graph.add_node(4, (4, 2, 2))
    # graph.add_edge(1,2,4)
    # graph.add_edge(3,1,2.3)
    # graph.add_edge(1,3,2.3)
    # graph.add_edge(3,4,2.3)
    # ga = GraphAlgo(graph)
    # ga.load_from_json("A2")
    # ga.plot_graph2()
    # ga.plot_graph()
    # xr = Range(0, 10)
    # yr = Range(0, 10)
    # dim = Range2D(xr, yr)
    # xr2 = Range(0, 1)
    # yr2 = Range(0, 1)
    # dim2 = Range2D(xr2, yr2)
    # r2r = Range2Range(dim2, dim)
    # x, y=r2r.world_to_frame(0.1,0.5)
    # print(x,y)
